<%= form_with(model: table, local: true) do |form| %>
  <% if table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(table.errors.count, "error") %> prohibited this table from being saved:</h2>

      <ul>
      <% table.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :chair %>
    <%= form.number_field :chair, id: :table_chair %>
  </div>

  <div class="field">
    <%= form.label :available %>
    <%= form.check_box :available, id: :table_available %>
  </div>

  <div class="field">
    <%= form.label :space_id %>
    <%= form.text_field :space_id, id: :table_space_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
